const assert = require('assert');
const nodeCommandLine = require('../index.js');

describe('nodeCommandLine', function () {
  it('has a test', function () {
    assert(false, 'nodeCommandLine should have a test');
  });
});
